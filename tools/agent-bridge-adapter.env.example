# Copy to a private file, for example:
#   ~/.config/agent-bridge/agent-bridge-adapter.env
# Never commit real tokens.

AGENT_BRIDGE_BASE=http://127.0.0.1:3010
AGENT_BRIDGE_TOKEN=replace-with-session-workspace-token

# Optional if openclaw is not on PATH.
# OPENCLAW_BIN=/usr/local/bin/openclaw
