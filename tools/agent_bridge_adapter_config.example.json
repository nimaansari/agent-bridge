{
  "auto_discover": true,
  "defaults": {
    "runtime": "openclaw_model",
    "max_session_turns": 0,
    "max_prompt_chars": 6000,
    "allow_session_rotation": false
  },
  "agents": [
    {
      "agent_name": "assistant",
      "runtime": "openclaw_model"
    },
    {
      "agent_name": "reviewer",
      "runtime": "custom",
      "command": [
        "hermes",
        "run",
        "--session",
        "{session_id}",
        "--message",
        "{message}"
      ]
    }
  ]
}
